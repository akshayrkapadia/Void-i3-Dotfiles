#!/bin/bash

OPTION=$(echo -e "Entire Screen\nSelection" | dmenu -p "Screenshot")

if [[ "$OPTION" == "Entire Screen" ]]; then
	scrot && notify-send "Screenshot saved to ~/Pictures/screenshots"
elif [[ "$OPTION" == "Selection" ]]; then
	scrot -s && notify-send "Screenshot saved to ~/Pictures/screenshots"
fi
